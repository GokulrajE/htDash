# htDash — CLAUDE.md

HOMER Therapy Dashboard (htDash) is a Flask-based clinical dashboard for managing the HOMER RCT therapy intervention. This document serves as the source of truth for the project context, architecture decisions, and the active refactoring plan to transition from a single-page application (SPA) to a URL-routed multi-page website. 

---

## Project Overview

**htDash** is a Flask-based clinical dashboard for the HOMER rehabilitation study. It manages stroke patients across three hospital sites (Manipal, Ranipet, Ludhiana). Core features:
- **Patient Management:** Patient enrollment, group assignment (experimental/control), and status tracking
- **Exercise Management:** Exercise prescriptions (VCG programs), ADL session recording, call logs
- **Analytics:** Usage charts per hospital site
- **Device Management:** Pluto & Mars device assignment and SIM card inventory
- **Data Sync:** AWS S3 sync for device config data and call records
- **Access Control:** Role-based access: global admin, site admin, regular user

**Stack:** Flask (Python) · Tailwind CSS · Vanilla JS · Chart.js · JSON/CSV file storage · AWS S3

---

## Active Refactoring: SPA → URL-Routed Website

### Branch: `urlrouted`

The original `main` branch implements the dashboard as a single-page application (one giant `dashboard.html` with JS-driven tab switching). This is being replaced with a fully URL-routed multi-page Flask application.

**Reason for change:**
- The app is fundamentally a CRUD tool — each section is largely independent
- Flask blueprints already define natural URL boundaries
- Browser back/forward, refresh, and deep-linking are broken in the SPA
- `dashboard.html` (39KB) is too large and hard to maintain
- No shared state justifies the SPA complexity

**Approach:**
- All work happens on the `urlrouted` branch
- `main` remains fully functional throughout
- Merge only when the new structure is complete and tested


---

## URL Structure

| Page           | URL                      | Notes               |
|----------------|--------------------------|---------------------|
| Main Page      | /                        | Main dashboard      |
| Patient list   | /patients                |                     |
| Patient detail | /patients/<homer_id>     |                     |
| Devices        | /devices                 |                     |
| SIM cards      | /sims                    |                     |
| Login Page     | /login                   | Login functionality |

---

## Data Storage

htDash generates data on the server and saves it to an AWS S3 bucket. During development a local `data/` directory is used instead. Local storage is disabled in production, which syncs directly to S3.

All files generated by htDash have a two-line preheader:
```
:Location: <hospital_name>
:HomerId: <homer_id_or_user_id>
```

### Data folder structure

```
data/
├── ranipet/
│   ├── patients/
│   │   ├── <homer_id1>/
│   │   ├── <homer_id2>/
│   │   └── ...
│   ├── dashboard/
│   │   ├── <user_id1>.csv
│   │   ├── <user_id2>.csv
│   │   └── ...
│   └── devices/
│       ├── pluto/
│       ├── mars/
│       ├── actigraphs/
│       ├── modems/
│       └── sims/
├── manipal/
└── ludhiana/
```

### Patient folders

Each patient folder contains a JSON metadata file, an activity log, and subfolders for clinical data. The subfolder set depends on group assignment.

#### Experimental group
```
patients/<homer_id>/
├── <homer_id>.json
├── <homer_id>.log
├── pluto/
├── mars/
├── actigraphs/
├── adl/
├── adverse_events/
│   └── attachments/
├── call_logs/
│   └── attachments/
└── timeline/
    └── attachments/
```

#### Control group
```
patients/<homer_id>/
├── <homer_id>.json
├── <homer_id>.log
├── vcg_exercise/
├── actigraphs/
├── adl/
├── adverse_events/
│   └── attachments/
├── call_logs/
│   └── attachments/
└── timeline/
    └── attachments/
```

#### Populating folders
The patient folder and all subfolders are created when a new patient is enrolled.
- `pluto/` and `mars/` — populated by devices uploading directly to S3
- `actigraphs/` — uploaded periodically by the study engineer
- `vcg_exercise/`, `adl/`, `adverse_events/`, `call_logs/`, `timeline/` — populated by the therapist via htDash


#### Patient data file `<homer_id>.json`

Each patient folder contains a single JSON file named after the patient ID. All fields are factual — status is never stored but always derived.

```json
{
  "homerID":                 "HOMRP001",
  "hospitalID":              "RP-HS-001",
  "group":                   "experimental",
  "trainingSide":            "Right",
  "enrollDate":              "2024-06-01T08:30:00",
  "activationDate":          "2024-06-03T09:15:00",
  "discontinuationDate":     null,
  "trainingCompletionDate":  null,
  "a0CompletionDate":        null,
  "a1CompletionDate":        null,
  "a2CompletionDate":        null
}
```
- `group`: `null` | `"experimental"` | `"control"`
- `trainingSide`: `"Right"` | `"Left"` | `null` (only set for experimental group)
- All date fields use ISO 8601 datetime format (`YYYY-MM-DDTHH:MM`). `null` means the event has not occurred.
- These dates fields cannot exceed the current local time! No future datetimes are allowed.

**Derived patient state (never stored):**

| `group` | `activationDate` | `discontinuationDate` | `trainingCompletionDate` | `a0CompletionDate` | `a1CompletionDate` | `a2CompletionDate` | Status               |
|---------|------------------|-------------------|--------------------------|--------------------|--------------------|--------------------|----------------------|
| null    | null             | null              | null                     | null               | null               | null               | unassigned           |
| set     | null             | null              | null                     | set                | null               | null               | inactive             |
| set     | set              | null              | null                     | set                | null               | null               | active               |
| set     | set              | null              | set                      | set                | null               | null               | training_completed   |
| set     | set              | null              | set                      | set                | set                | null               | a1_completed         |
| set     | set              | null              | set                      | set                | set                | set                | all_completed        |
| null    | null             | set               | null                     | set                | null               | null               | pre_discontinued     |
| set     | set              | set               | x                        | x                  | x                  | x                  | discontinued         |


#### `<homer_id>.log`

Records all clinical actions taken on this patient. The unit of audit is the patient — user activity is derived by searching patient logs for a given user ID.

```
:Location: Ranipet
:HomerId: HOMCMCV001
[2026-03-19 09:45:00]   siva    #1    Created new patient : HOMCMCV001.json
[2026-03-19 10:12:00]   siva    #1    Group assigned: experimental
[2026-03-19 14:05:00]   siva    #2    ADL session recorded — 3 exercises | adl/2026-03-19.json
```

- **Timestamp** — ISO 8601 datetime format
- **User** — `loginid` of the user who performed it
- **`#N`** — session number from that user's `<user_id>.csv`
- **Action text** — human-readable description; `|` separates the action from the filename where full details can be found

The log message and server actions for each page action are described in the `Actions` section under each page.

### dashboard folder

One CSV per user, created on first login. Records every login session.

#### `<user_id>.csv`

```
:Location: Ranipet
:ID: <user_id>
session_id, login_time, logout_time, logout_reason
1, 2026-03-19 09:42:11, 2026-03-19 11:30:45, user_initiated
2, 2026-03-19 15:00:00, null, null
```

- `session_id` — auto-incrementing integer, unique per user
- `login_time` — datetime of successful login
- `logout_time` — datetime session ended (`null` if session ended without detection)
- `logout_reason` — `user_initiated` | `null`

**Logout detection:**
- `user_initiated` — user clicks Sign Out

Sessions closed without detection (browser crash, closed tab, server restart) leave `null` in both fields — this is intentional and honest.

### timeline folder 
Contains different files corresponding to different events.
| filename | details |
|----------|---------|
| prediscontinuation.json | Details of a pre-discontinuation action |
| discontinuation.json | Details of the patient discontinuation |
| protocol_events.json | Details of the protocol triggered events completed for the patient|

There is an attachment folder where any attachments are stored with the appropriate name with datetime (ISO 8601 format), which must be referended in one of the .json files in the root of this folder. There can be no orphaned attachments in the attachment folder.

---

## Template Architecture

### Base Template
A single `base.html` will contain:
- Sidebar with navigation links (`<a href>` not JS onclick)
- Header bar
- Flash message display
- Block definitions: `title`, `content`, `scripts`

All page templates extend `base.html`.

### Route Organisation
Page-rendering routes live in the same blueprint that owns the domain logic. The blueprint already has the right URL prefix. A `GET /` route inside a blueprint with `url_prefix='/devices'` becomes `GET /devices/`.

| Template             | Route                      | Blueprint file              |
|----------------------|----------------------------|-----------------------------|
| `dashboard.html`     | `GET /`                    | `main.py`                   |
| `login.html`         | `GET /login`               | `main.py`                   |
| `patients.html`      | `GET /patients`            | `routes/user_management.py` |
| `patient_detail.html`| `GET /patients/<homer_id>` | `routes/user_management.py` |
| `devices.html`       | `GET /devices/`            | `routes/devices.py`         |
| `sims.html`          | `GET /sim_cards/`          | `routes/sim_cards.py`       |

### `GET /login` Page
#### Actions

**Successful login**
- Trigger: User submits correct credentials
- Log message: None
- Server actions:
  - Enter a row to `<user_id>.csv`

**Failed login**
- Trigger: User submits incorrect credentials
- Log message: None
- Server actions: None

### `GET /` Page
- Main dashboard with that display the bubble contains various study level numbers. These are:
  - Total number of patients enrolled in the study
  - Total number of patients in the experimental group
  - Total number of patients in the control group
  - Total number of patients unassigned to any group
  - Total number of patients who are inactive
  - Total number of patients who have completed training
  - Total number of patients who have completed a1 assessment
  - Total number of patients who have pre-discontinued from the study
  - Total number of patients who have discontinued from the study
These numbers are for the study site the person has access to. They cannot see the numbers for the other sites.
- The numbers for the derived by reading the individual <homer_id>.json files under the patients folder and aggregating the data.
- There are no other UI elements.

#### Actions
None

### `GET /patients` Page

Displays the list of patients for the user's site. Admins see all patients; site users see only their site.

#### Page elements
- Search bar (filter by patient ID)
- Add Patient button (site admin and global admin only)
- Filter tabs: All, Unassigned, Inactive, Active, Training Complete, A2 Compelte, All Complete, Pre-Discontinued, Discontinued — each showing a count.
- "All" filter tab must be selected by default.
- When any of the filter button is clicked, the corresponding list is shown.
- Any shown list should be sorted by their `enrollDate`.
- Each subject "row" should display differnet patients details. The color of the patient's indicates group.
- A status badge indicates status, which has different colors for different status.
- Patient list — each row shows: Homer ID, Hospital ID, Group, Training Side, derived Status
- Clicking a patient navigates to `/patients/<homer_id>`

#### Patient state transitions

| Current state      | Action                        | Field set                              | Next state         | 
|--------------------|-------------------------------|----------------------------------------|--------------------|
| unassigned         | Assign group <br>Record a0 date                  | `group` <br>`a0CompletionDate`                               | inactive           |
| unassigned         | Discontinue (pre-enrolment)   | `discontinuationDate`                  | pre_discontinued   |
| inactive           | Activate                      | `activationDate` | active             |
| inactive           | Discontinue                   | `discontinuationDate`                  | discontinued       |
| active             | Complete training             | `trainingCompletionDate`               | training_completed |
| active             | Discontinue                   | `discontinuationDate`                  | discontinued       |
| training_completed | Record a1 date                | `a1CompletionDate`                     | a1_completed       |
| training_completed | Discontinue                   | `discontinuationDate`                  | discontinued       |
| a1_completed       | Record a2 date                | `a2CompletionDate`                     | all_completed      |
| a1_completed       | Discontinue                   | `discontinuationDate`                  | discontinued       |

**Notes:**
- `a0` is a baseline assessment compulsarily recorded during the Activate action. This needs to be done through a datetime selector in the dialog for assigning group.
- `discontinued` is a terminal state reachable from any non-completed state via `discontinuationDate`.
- `pre_discontinued` and `all_completed` are terminal states.


#### Actions

**Add Patient** *(admin only)*
- Trigger: "Add Patient" button in the page header
- Modal fields:
  - Hospital Patient ID (text input, required)
  - Training Side (Left / Right toggle, required)
- Log message: `Created new patient : <homer_id>.json`
- Server actions:
  - Create `<homer_id>.json`
  - Create `<homer_id>.log` with this entry as the first line

**Assign Group** *(admin only, unassigned patients)*
- Trigger: "Assign" button on patient card
- Modal fields:
  - Group (Experimental / Control toggle, required)
  - A0 Assessment Date (datetime selector, required)
- Log message: `Assigned group to <group>`
- Server actions:
  - Update `<homer_id>.json` with `group` and `a0CompletionDate`

**Pre-Discontinue** *(admin only, unassigned patients)*
- Trigger: "Pre-DC" button on patient card
- Modal fields:
  - Reason / Comments (textarea, required)
- Log message: `Pre-discontinued patient : prediscontinuation.json`
- Server actions:
  - Create `prediscontinuation.json` in `timeline/`
  - Update `<homer_id>.json` with `discontinuationDate`

---

## What stays as JS
- Patient detail sub-tabs (contextual UI within one page)
- Chart.js rendering
- QR code generation
- Modal interactions (add patient, assign device, etc.)
- In-page dynamic updates (fetch calls within a page)

---

## Implementation Order

1. **Define URL structure and data flow** (this document, with user input)
2. **Create `base.html`** — sidebar, header, navigation as real links
3. **Dashboard page** — summary stats, simplest page to start
4. **Patients list page** — patient table/list
5. **Patient detail page** — port the detail panel with its JS sub-tabs
6. **Devices page**
7. **SIMs page**
8. **Cleanup** — remove old `dashboard.html` and unused JS
9. **Test all routes and functionality**
10. **Merge to `main`**

---

## Key Files Reference

| File | Purpose |
|---|---|
| `main.py` | App entry point, blueprint registration |
| `config.py` | Credentials, device types, exercise library, AWS settings |
| `processinInstance.py` | S3 sync, auto-activation logic |
| `models/user.py` | Session proxy for per-request user context |
| `routes/user_management.py` | Patient CRUD, group assignment, device config uploads |
| `routes/patient_events.py` | Issues and adverse event logging |
| `routes/exercises.py` | Exercise prescriptions, VCG program management |
| `routes/charts.py` | Usage analytics |
| `routes/auth.py` | Login, logout, rate limiting |
| `routes/devices.py` | Device set management |
| `routes/call_logs.py` | Call record logging |
| `routes/adl_exercises.py` | ADL session recording |
| `utils/file_handlers.py` | Atomic JSON/CSV file I/O |
| `utils/s3_operations.py` | AWS S3 operations |
| `utils/data_processors.py` | Analytics helpers |
| `templates/dashboard.html` | **[TO BE REPLACED]** Current monolithic SPA template |
| `static/js/app/` | Modular JS — parts will be retained, parts removed |

---

## Conventions & Constraints

- Do not modify the `main` branch during this refactor
- Keep all existing API endpoint URLs intact (backend routes are not changing)
- Preserve exact visual appearance (Tailwind classes, layout) during conversion
- Do not add new features during this refactor — functionality parity only
